# NetBuildApp HANDOFF

## 位置づけ（v1.8で統合）

**RouteHQApp は NetBuildApp に統合済み。別リポジトリは不要。**
実機診断のクラス（DeviceNetworkCollector / ProbeRunner / SubnetCalc）をパッケージごと移植し、
`RealDiagnosis` でゲームの観点に接続した。社員メニューの「いま使っているネットワークを見る」から実行。

- 設定を見るだけ（通信しない）と、疎通も測る（ping / 名前解決 / TCP）の2モード
- 実測結果はゲームと同じ NetGraph に入るので、判定の目線が揃う
- 実機の所見をゲームの論点に結びつける文言は `RealDiagnosis.review()` に集約。
  DNS単一障害点・サブネット収容数・VPN経由・来客SSID の4つを扱う
- 他アプリから NetGraph JSON を受け取る口（DeviceReview + ACTION_SEND）は残してある
- v1.9: `RealQuiz` が測定値から出題する。ゲートウェイのアドレス・DNSの台数・サブネット・
  VPN経由・来客SSID・キャプティブポータル・実測の失敗内容を材料に、当てはまるものだけ出す。
  問題を増やすときは `RealQuiz.build()` に条件つきで1問足す

## 旧・位置づけ

ネットワーク構築ゲーム本体（学習目的）。実機測定アプリ **RouteHQApp** とは別リポジトリで、
共有するのは `NetGraph` スキーマ（schema: netgraph/1）のみ。

- RouteHQApp = 実端末の状態を NetGraph にする
- NetBuildApp = ゲーム内の設計を NetGraph にする + どちらも同じ `RuleEngine` で評価する

RouteHQApp の「JSON共有」から本アプリを選ぶと、実測データがそのまま評価対象になる
（`ACTION_SEND` / text/plain のインテントフィルタで受信）。

## 現在のバージョン

v1.9

## 実装済み

- `NetGraph` — 共通スキーマ。`fromJson` で RouteHQApp の出力を読める
- `FirewallRule` — source/dest ゾーン、protocol、port、action、優先順位。先にマッチしたルールが勝つ（MD §14）
- `RuleEngine` — BFS による `canReach(src, dst)`（MD §13）
  - L2スイッチは VLAN が一致する場合のみ中継（MD §16）
  - Firewall ノード通過時にゾーン間ルールを評価。どれにも当たらなければ暗黙 Deny
- `Design` — プレイヤーの選択（来客VLAN分離 / DMZ / 来客Denyルール / サブネット幅）から構成グラフと FWルールを生成。コスト計算つき
- `Scenario` — 案件1件（サンプル商事）。表面要求と隠れた要求3件を分離して保持（MD §8 / §9）
- `Evaluator` — 設計レビュー（MD §30）と案件適合度スコア（MD §32）
- `Incident` / `IncidentEngine` — 設計の弱点を種にした障害生成（MD §25）。弱点由来の原因は重み3倍
- `Diagnostics` — 6種の診断コマンドと仮説確率のベイズ更新（MD §27）。結果は原因から決定的に決まる
- `TopologyView` — ステータス盤の構成図。設計に応じて線の色と危険経路（赤い破線）が変わり、障害中は該当リンクが赤く点滅する。触る場所ではなく見る場所
- `GameState` — Day・信頼・設計・進行中の障害・過去の設計起因障害を SharedPreferences に保存。再起動しても続きから遊べる
- `DeviceReview` — 実測 NetGraph に対する指摘（デフォルトルート、DNS冗長性、VPN、キャプティブポータル、IP収容数、切り分け）

## 検証済みの挙動

| VLAN分離 | DMZ | 来客→社内PC | インターネット→社内PC |
|---|---|---|---|
| なし | なし | 到達（危険） | 到達（危険） |
| あり | なし | 遮断 | 到達（危険） |
| なし | あり | 到達（危険） | 遮断 |
| あり | あり | 遮断 | 遮断 |

来客Denyルールだけを入れて VLAN 分離をしない場合、通信はそもそも Firewall を通らないため
ルールは効かない。この場合は「効いていない」ことを指摘として表示する。

## 診断コマンドの識別力（検証済み）

| 真の原因 | ping GW / ping 8.8.8.8 / 名前解決 の3つだけで特定できるか |
|---|---|
| リンク断 | できる（95%） |
| 回線障害 | できる（90%） |
| DNS停止 | できる（82%） |
| IP枯渇・侵入・改ざん | できない（33%で並ぶ） |

後半3つは DHCP確認・社内ログ・公開サーバーログを打たないと切り分かない。
「通信系のコマンドだけ打っていても分からない障害がある」ことを体験させる意図的な設計。

## 運用フェーズのループ（v0.3）

1. 設計 → 稼働（Day 1〜）
2. 障害発生。設計の弱点由来の原因は重み3倍
3. 診断 → 原因回答 → 正解なら信頼+、弱点由来なら `occurredFaults` に記録
4. 直さずに放置すると、その原因の重みがさらに2倍（合計6倍）になり再発表示が出る
5. 稼働後に設計を直すと、増分の50%が割増として `extraCost` に積まれ、予算判定に反映される

「安く済ませて後から直す」が最も高くつくように作ってある。

## 画面構成（v0.5）

出しっぱなしにするのは4つだけ。それ以外はすべてタップで開くダイアログ。

1. 構成図（TopologyView）— 設計に連動。危険な到達経路は赤い破線、障害中は該当リンクが点滅
2. 状態帯 — Day / 信頼 / 費用 / 稼働状態
3. 自社の社員（左下）— 押すと行動メニュー（翌日へ / 診断 / 原因報告 / 設計レビュー / FWルール / 案件状況）。長押しでリセット
4. クライアント（右下）— 押すと要望・性格・未確認要望のヒント。そこから「聞いてみる」でヒアリング

中央下の吹き出しには顧客の最新の一言だけを出す。1行のガイド文が状況に応じて切り替わる。

### 図の直接編集

ノードとリンクはタップ判定を持つ（TopologyView.OnPickListener）。

| 触る場所 | 変えられること |
|---|---|
| スイッチ / 来客端末 | 来客VLAN分離 |
| Webサーバー | DMZ / 内部セグメント |
| Firewall | 来客Denyルール（現在のルール一覧も表示） |
| 社員PC | 内部サブネット /24 ↔ /26 |
| インターネット | 変更不可。判定がどこで決まるかの説明のみ |
| 各配線 | その区間の意味と現状の説明 |

### 画面の余白と向き

- targetSdk 35 はエッジツーエッジが既定のため、`ViewCompat.setOnApplyWindowInsetsListener` で
  systemBars + displayCutout ぶんのパディングをルートに与えている。上下が切れる場合はここを確認
- 構成図は縦横で座標セットを切り替える（Spot に portrait / landscape の2組を保持）
- 図は画面いっぱいには広げない。中央寄せの箱に収め、最大サイズは縦 400x520dp / 横 660x330dp。
  機器が増えたらこの上限を上げる
- ノードのタップ判定は半径30dp、配線は距離18dp

### 機器を増やす土台（v1.1）

図はグラフから組み立てる方式に変わった。`TopologyView.rebuild()` が `Design.buildGraph()` の
ノードとエッジを読み、`SLOTS`（id → アイコンと座標の登録表）に載っているものだけを描く。
線の色と障害時の点滅も、ノードのゾーンと種別から自動で決まる。

**機器を1つ増やす手順（v1.1以降）**

1. `DeviceGuide.entries()` に説明を追加（status は PLANNED のままでよい）
2. `Design` に選択肢のフィールドとコスト定数を追加し、`buildGraph()` にノードとエッジ、
   必要なら `buildRules()` にルールを追加
3. `TopologyView.SLOTS` に1行追加（id・アイコン・縦横の座標）
4. `Evaluator` に加点／減点の判定を追加し、`bestDesign()` の総当たりループに次元を1つ足す
5. `MainActivity.nodeMenu()` にタップ時の切り替えを追加、`explain()` に新人の解説、
   `nextAdvice()` に簡単モードの助言を追加
6. `GameState` の save/load に項目を追加
7. `tools/build_docs.sh` で docs を再生成

DNSサーバー（v1.1）がこの手順の実例。差分を追えば次の機器も同じ形で足せる。

### 署名鍵の固定（v1.7.2）

GitHub Actions の debug ビルドは、ランナー上でその都度生成される鍵で署名されるため、
毎回署名が変わり、更新インストールができなかった（アンインストールが必要だった）。

`keystore/debug.keystore`（PKCS12・alias androiddebugkey・パスワード android）を
リポジトリに含め、`signingConfigs.debug` で固定した。以後は上書きインストールできる。

- この鍵はデバッグ配布専用。Google Play へ出す場合は別の鍵を作り、GitHub Secrets に置く
- 鍵を差し替えると、そのタイミングで再度アンインストールが必要になる
- v1.7.2 への更新時のみ、これまでの鍵と変わるので1回だけアンインストールが要る
- 同じ手順を他アプリに入れる場合、keystore ごとコピーすれば同じ鍵で署名される

### ビルド確認（v1.7.1）

初回のCIビルドで `Evaluator.describeRules` がコンパイルエラーになった。
`design.buildRules()` を `buildRules(scenario)` に一括置換したとき、scenario を引数に持たない
メソッドまで巻き込んでいたため。`describeRules(Scenario, Design)` に変更して解消。

同種のスコープ漏れが他に無いことは、全メソッドの変数スコープを走査して確認済み。

### 段階7：SASE（v1.7）

境界をどこに置くかは **案件の前提**（`Scenario.Boundary`）。プレイヤーは選べない。
`Design.buildGraph(scenario)` / `buildRules(scenario)` / `cost(scenario)` が前提で分岐する。

2件目の案件 `Scenario.distributed()`（合同会社リモートワークス、予算800,000、SASE前提）を追加。
社員メニューの「案件を切り替える」で選ぶ。進行は `GameState` のキーを案件IDで分けて別々に保存。

SASE案件で問われるのは機器の置き方ではなく2点だけ。

| 選択 | 費用 | 判定 |
|---|---|---|
| 検査の例外をなくす | +50,000 | 例外を残すと迂回経路ができる（-20）、なくすと +20 |
| 事務所システムへSASE経由の認証つきアクセス | +120,000 | 無いと要求未達（-15） |

VLAN・DMZ・プロキシ・VPNの判定はSASE案件では行わない。満点は132点（予算800,000）。

**新しい案件を足す手順**: `Scenario` に1メソッド追加し、`Scenario.all()` に登録する。
ON_PREM ならそのまま動く。SASE なら `Design.buildSaseGraph()` / `saseRules()` が使われる。
3つ目の境界方式を足す場合は、この2メソッドと同じ組を作り、`Evaluator` の分岐に1本足す。

### 段階6：VPN / クラウド / ベンダー接続（v1.6）

ゾーンが7つになった（guest / internal / dmz / internet / remote / vendor / cloud）。
判定基準はすべて Scenario 側にあるので、Evaluator は無変更で対応できた。

追加した選択肢

| 選択 | 費用 | 判定 |
|---|---|---|
| リモートアクセスVPN | +120,000 | 無いと「在宅から社内システムへの接続」が要求未達（-15） |
| 保守接続を必要時のみに | +30,000 | 常時だと vendor→pc が到達し危険（-20）。申請制なら遮断で +15 |

クラウドは設計で置き換えられないノードとして図に出す。社員からクラウドへの通信は
「必要な許可」に入っているので、それを塞ぐと過剰許可ではなく要求未達になる。

隠れた要求が6件に増えた（在宅勤務、保守業者の接続方式を追加）。

満点は 170点（交渉なし・予算超過のまま）／207点（2回交渉して +400,000 を得た場合）。
全部の守りを入れると 1,250,000 円かかるので、満点には2回の交渉が要る。
信頼55以上で1回 +200,000 なので、障害対応で信頼を保つことが前提になる。

### 案件が判定基準を持つ構造（v1.5）

判定に使う「その案件で必要な通信」と「通ってはいけない経路」を `Scenario` へ移した。
`Evaluator` に案件固有の知識は残っていない。

```
Scenario.allowances   業務上必要な通信（ゾーン対 + 到達性を測るノード対 + 根拠の要求番号）
Scenario.prohibitions 通ってはいけない経路（ノード対 + 減点 + 遮断できていたときの加点）
Scenario.zones        この案件に登場するゾーン。ルール編集の選択肢にもなる
```

判定は3方向になった。

1. allowance が通っていない → 要求未達（減点）
2. allowance が通っていて、根拠の要求も確認済み → 加点
3. allowance が通っているが根拠が未確認 → 加点しない。「注意」として指摘だけ出す
4. allowance に無い Allow ルール → 過剰許可（減点）

3 が MD §9 の趣旨。たまたま通っている状態と、聞いて判断した状態を区別する。

**新しい案件を1件足す手順**: `Scenario` を1つ書けば、要求・許可・禁止・ゾーン・満点まで決まる。
機器の選択肢（Design）はまだ共通なので、そこは次の課題。

満点は移設前と同じ 147点／167点（回帰確認済み）。

### Firewallルールの手編集（v1.4）

`Design.customRules`（null なら `defaultRules()` を使う）。Firewall から編集モードに入ると
自動生成ルールがコピーされ、追加・削除・順序入れ替えができる。保存は GameState の rules 配列。

`Evaluator.reviewRules()` が2種類の問題を検出する。

| 検出 | 判定方法 | 点数 |
|---|---|---|
| 過剰な許可 | NEEDED_ALLOW（guest→internet / internal→internet / internal→dmz / internet→dmz）以外の Allow | -8 |
| 効かないルール | そのルールが受け持つゾーンの組が、上のルールで全部拾われている | -5 |
| 無駄も抜けもない | 上の2つが0件 | +10 |

判定は `FirewallRule.coveredPairs()`（ZONES 4種の総当たり）で行う。
ゾーンを増やすときは `FirewallRule.ZONES` と `NEEDED_ALLOW` の両方を更新すること。

満点は 147点（交渉なし）／167点（増額後）。

### 予算交渉（v1.3）

守りを全部入れると 1,100,000 円かかり、初期予算 1,000,000 円では足りない。
決裁者から増額を引き出す設計にした（`GameState.extraBudget` / `negotiations`）。

| 条件 | 結果 |
|---|---|
| 残っている「危険」「将来リスク」の指摘が0件 | 却下（出す理由が説明できない） |
| 信頼 40未満 | 却下。交渉回数だけ消費する |
| 信頼 40〜54 | +100,000 円 |
| 信頼 55以上 | +200,000 円 |
| 交渉は2回まで | 3回目以降は一律で断られる |

満点は予算に連動する。交渉なし 137点／増額後 157点。
障害対応で稼いだ信頼が予算に変わる循環になっている。

### 今後のステージ計画

序盤は選択肢を絞り、機器が増えるにつれて判断の質を問う方向に上げていく。

| 段階 | 追加する機器 | 求める判断 |
|---|---|---|
| 1（実装済み） | スイッチ / Firewall / 公開サーバー | VLAN分離、DMZ、サブネット幅 |
| 2（実装済み） | DNSサーバー | 単一障害点をどこまで許容するか |
| 3（実装済み） | 社内サーバー | 公開サーバーと同居させない。ゾーン設計の基本 |
| 4（実装済み） | プロキシ | 外向き通信を集約するか。記録が無いと切り分けられないことの体験 |
| 5（実装済み） | Firewallルールの手編集 | 順序と暗黙Denyを自分で組む。過剰許可と効かないルールの検出 |
| 6（実装済み） | VPN / クラウド / ベンダー接続 | VPNが要るかの判断。常時接続の是非 |
| 7（実装済み） | SASE | 案件の前提として与えられる。検査の網羅と認可が判断の中心になる |

段階6（VPN / クラウド / ベンダー接続）の準備は v1.5 で完了している。
`Scenario.zones` に vendor / cloud を足し、`allowances` に保守用の許可を1件書けば、
過剰許可の判定はその案件だけ緩む。Evaluator は触らなくてよい。

### マニュアルと機器図鑑（v1.0）

- `Manual.java` — アプリ内マニュアル全9章。章を足すときは `sections()` に1件追加
- `DeviceGuide.java` — 機器の簡易説明。`entries()` に1件追加するだけで増える。
  status を PLANNED にしておけば、図に出さずに説明だけ読める状態にできる
- 入口は3つ: 社員メニューの「マニュアルを読む」「機器の説明を読む」、
  図の機器を押したときのダイアログの「この機器について」
- `tools/build_docs.sh` を実行すると、Java の本文から `docs/MANUAL.md` と `docs/DEVICES.md` を再生成する。
  **本文の原本は Java 側**。MD を直接編集しても次の生成で消えるので注意

機器を1つ増やす手順:
1. `DeviceGuide.entries()` に1件追加（status は最初 PLANNED）
2. 図に出す段になったら `Design.buildGraph()` にノードを追加し、`TopologyView` に座標とリンクを追加
3. 判定に関わるなら `RuleEngine` のゾーン／VLAN の扱いを確認
4. `tools/build_docs.sh` で docs を再生成

### 簡単モード（v0.9）

社員メニューから切り替え。`GameState.easyMode` に保存される。

- 依頼者が「ネットワークに詳しい担当者」になり、右に立つ相手は常に依頼者に固定される
- 話しかけるたびに、現在の状態から**次にやること1つ**だけを返す（`nextAdvice`）
  1. 未確認の要望があれば → 聞くべき質問文をそのまま提示
  2. 障害中で確率80%未満 → 次に打つ診断コマンドを提案（候補を最も均等に割れるもの＝`Diagnostics.suggest`）
  3. 障害中で確率80%以上 → 原因を報告するよう促す
  4. 設計が最良と違う → 差分を1項目、理由つきで提示
  5. すべて一致 → 満点であることを伝える
- ダイアログには「その話を聞く」「診断する」ボタンが状況に応じて出るので、助言からそのまま実行できる

### 点数と満点

`Evaluator.bestDesign` が16通りを総当たりして最良設計を求め、`maxFitness` が満点を返す。

- 確認できた要求ごとに +5 点（聞かないと満点にならない）
- この案件の満点は **112 点**（VLAN分離 + DMZ + /24 + 全ヒアリング + 割増なし）
- 何もしない初期状態は -35 点
- 同じ最良設計でも、稼働後に直すと割増で 75 点まで落ちる

### 登場人物（v0.8）

画面に同時に出すのは常に2人まで。左＝自社側（Ally）、右＝先方（Stakeholder）。

左（Ally）

| 人物 | 役割 |
|---|---|
| 自社の社員 | 行動の実行。翌日へ / 診断 / 原因報告 / 設計レビュー / FWルール / 案件状況 |
| 自社の新人 | 用語の解説役。社員メニューの「新人に交代する」で入れ替わる。VLAN・DMZ・暗黙Deny・サブネット・現在の危険箇所を、いまの設計の状態に合わせて説明する |

右（Stakeholder）— 上から順に条件判定

| 相手 | 出る条件 | 持っている情報 |
|---|---|---|
| 現場担当（情シス） | 障害発生中 | 障害の一次情報、これまでの診断ログ、仮説確率 |
| 決裁者（上司） | 予算超過 or 信頼40未満 | 金額の評価、設計起因障害の累積 |
| インターン（学生スタッフ） | 来客→社内が到達可能 | 私物端末を繋ぎたがる。設計の穴が実害になる筋道を提示 |
| 総務担当（運用担当） | 未確認の要望あり | 現場の観察ヒント |
| 依頼者（発注担当） | 上記以外 | 表面要求、性格、ヒアリング |

インターンは「悪意のない人が設計の穴を踏む」役。RuleEngine の canReach(guest→pc) をそのまま登場条件にしている。
表情は全員 normal / worry / angry を状態から自動切替。

### 画像アセット

- `chara_normal` / `chara_worry`（障害中）/ `chara_angry`（信頼35未満）= 自社の社員。状態で自動切替
- 自社の社員: `chara_normal` / `chara_worry` / `chara_angry`
- 依頼者: `chara_client` / `chara_client_worry` / `chara_client_angry`
- 決裁者: `chara_boss_normal` / `chara_boss_worry` / `chara_boss_angry`
- 現場担当: `chara_it_normal` / `chara_it_worry` / `chara_it_angry`
- 総務担当: `chara_office_*` / インターン: `chara_intern_*` / 自社の新人: `chara_newbie_*`
- 背景透過は最大連結成分のみ残す処理で行っている（装飾の星が混ざるため）
- 吹き出しは依頼者の左に置き、`bubble_tail`（右向きの三角）で発話元を示す
- ノードアイコンは VectorDrawable（ic_pc / ic_guest / ic_switch / ic_server / ic_cloud）、Firewall のみ PNG（node_firewall）

## 次の候補（v0.5 以降）

1. 顧客性格（MD §7）— 同じ質問への回答を属性で変える
2. 案件の複数化と動的生成（MD §38）
3. 障害中の一時的な到達性変化 — 現状 RuleEngine は設計のみを見ており、障害は診断結果としてしか現れない
4. 実機 NetGraph を案件の初期構成として取り込むモード
5. LLM 接続は最後。会話生成だけを担当させ、判定は RuleEngine のまま維持する

## ビルド

GitHub Actions（.github/workflows/build.yml）で debug APK を出力。ローカルビルドはしない。
