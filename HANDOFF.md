# NetBuildApp HANDOFF

## 位置づけ

ネットワーク構築ゲーム本体（学習目的）。実機測定アプリ **RouteHQApp** とは別リポジトリで、
共有するのは `NetGraph` スキーマ（schema: netgraph/1）のみ。

- RouteHQApp = 実端末の状態を NetGraph にする
- NetBuildApp = ゲーム内の設計を NetGraph にする + どちらも同じ `RuleEngine` で評価する

RouteHQApp の「JSON共有」から本アプリを選ぶと、実測データがそのまま評価対象になる
（`ACTION_SEND` / text/plain のインテントフィルタで受信）。

## 現在のバージョン

v0.5

## 実装済み

- `NetGraph` — 共通スキーマ。`fromJson` で RouteHQApp の出力を読める
- `FirewallRule` — source/dest ゾーン、protocol、port、action、優先順位。先にマッチしたルールが勝つ（MD §14）
- `RuleEngine` — BFS による `canReach(src, dst)`（MD §13）
  - L2スイッチは VLAN が一致する場合のみ中継（MD §16）
  - Firewall ノード通過時にゾーン間ルールを評価。どれにも当たらなければ暗黙 Deny
- `Design` — プレイヤーの選択（来客VLAN分離 / DMZ / 来客Denyルール / サブネット幅）から構成グラフと FWルールを生成。コスト計算つき
- `Scenario` — 案件1件（サンプル商事）。表面要求と隠れた要求3件を分離して保持（MD §8 / §9）
- `Evaluator` — 設計レビュー（MD §30）と案件適合度スコア（MD §32）
- `Incident` / `IncidentEngine` — 設計の弱点を種にした障害生成（MD §25）。弱点由来の原因は重み3倍
- `Diagnostics` — 6種の診断コマンドと仮説確率のベイズ更新（MD §27）。結果は原因から決定的に決まる
- `TopologyView` — ステータス盤の構成図。設計に応じて線の色と危険経路（赤い破線）が変わり、障害中は該当リンクが赤く点滅する。触る場所ではなく見る場所
- `GameState` — Day・信頼・設計・進行中の障害・過去の設計起因障害を SharedPreferences に保存。再起動しても続きから遊べる
- `DeviceReview` — 実測 NetGraph に対する指摘（デフォルトルート、DNS冗長性、VPN、キャプティブポータル、IP収容数、切り分け）

## 検証済みの挙動

| VLAN分離 | DMZ | 来客→社内PC | インターネット→社内PC |
|---|---|---|---|
| なし | なし | 到達（危険） | 到達（危険） |
| あり | なし | 遮断 | 到達（危険） |
| なし | あり | 到達（危険） | 遮断 |
| あり | あり | 遮断 | 遮断 |

来客Denyルールだけを入れて VLAN 分離をしない場合、通信はそもそも Firewall を通らないため
ルールは効かない。この場合は「効いていない」ことを指摘として表示する。

## 診断コマンドの識別力（検証済み）

| 真の原因 | ping GW / ping 8.8.8.8 / 名前解決 の3つだけで特定できるか |
|---|---|
| リンク断 | できる（95%） |
| 回線障害 | できる（90%） |
| DNS停止 | できる（82%） |
| IP枯渇・侵入・改ざん | できない（33%で並ぶ） |

後半3つは DHCP確認・社内ログ・公開サーバーログを打たないと切り分かない。
「通信系のコマンドだけ打っていても分からない障害がある」ことを体験させる意図的な設計。

## 運用フェーズのループ（v0.3）

1. 設計 → 稼働（Day 1〜）
2. 障害発生。設計の弱点由来の原因は重み3倍
3. 診断 → 原因回答 → 正解なら信頼+、弱点由来なら `occurredFaults` に記録
4. 直さずに放置すると、その原因の重みがさらに2倍（合計6倍）になり再発表示が出る
5. 稼働後に設計を直すと、増分の50%が割増として `extraCost` に積まれ、予算判定に反映される

「安く済ませて後から直す」が最も高くつくように作ってある。

## 画面構成（v0.5）

出しっぱなしにするのは4つだけ。それ以外はすべてタップで開くダイアログ。

1. 構成図（TopologyView）— 設計に連動。危険な到達経路は赤い破線、障害中は該当リンクが点滅
2. 状態帯 — Day / 信頼 / 費用 / 稼働状態
3. 自社の社員（左下）— 押すと行動メニュー（翌日へ / 診断 / 原因報告 / 設計レビュー / FWルール / 案件状況）。長押しでリセット
4. クライアント（右下）— 押すと要望・性格・未確認要望のヒント。そこから「聞いてみる」でヒアリング

中央下の吹き出しには顧客の最新の一言だけを出す。1行のガイド文が状況に応じて切り替わる。

### 図の直接編集

ノードとリンクはタップ判定を持つ（TopologyView.OnPickListener）。

| 触る場所 | 変えられること |
|---|---|
| スイッチ / 来客端末 | 来客VLAN分離 |
| Webサーバー | DMZ / 内部セグメント |
| Firewall | 来客Denyルール（現在のルール一覧も表示） |
| 社員PC | 内部サブネット /24 ↔ /26 |
| インターネット | 変更不可。判定がどこで決まるかの説明のみ |
| 各配線 | その区間の意味と現状の説明 |

### 画像アセット

- `chara_normal` / `chara_worry`（障害中）/ `chara_angry`（信頼35未満）= 自社の社員。状態で自動切替
- `chara_client`（VectorDrawable のシルエット）= クライアントの暫定画像。差し替える場合は
  `res/drawable-nodpi/chara_client.png` を置いて `res/drawable/chara_client.xml` を削除する
- ノードアイコンは VectorDrawable（ic_pc / ic_guest / ic_switch / ic_server / ic_cloud）、Firewall のみ PNG（node_firewall）

## 次の候補（v0.5 以降）

1. 顧客性格（MD §7）— 同じ質問への回答を属性で変える
2. 案件の複数化と動的生成（MD §38）
3. 障害中の一時的な到達性変化 — 現状 RuleEngine は設計のみを見ており、障害は診断結果としてしか現れない
4. 実機 NetGraph を案件の初期構成として取り込むモード
5. LLM 接続は最後。会話生成だけを担当させ、判定は RuleEngine のまま維持する

## ビルド

GitHub Actions（.github/workflows/build.yml）で debug APK を出力。ローカルビルドはしない。
